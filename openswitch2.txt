| openswitch
| proxmox


у нас есть физ карта. на ней ип адресов. на нее навешен опенсвич vmbr0
на нем куча л2 портов с виланами. 
я хочу добавить на этот свич л3 порт в вилане. дать ему ип. 
и тогда я смогу программами которые крутсятс на хосте стучаться на ип адреса в этом
вилане в том числе и на виртуалки котоыре сидят на л2 портах этого свича


# ovs-vsctl add-port vmbr0 tempPort tag=805 -- set interface tempPort type=internal

# ip addr add 10.10.60.198/24 dev tempPort

# ip link set tempPort up




vmbr0 = имя опенсвич свича
tempPort = имя порта который мы создаем


