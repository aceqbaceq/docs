| git 
| branch
| clone

как с удаленного компа склонировать только оопреедленную ветку


	$ git clone --depth 1 --branch <tag_name> <repo_url>


это мега полезно еси  репозиторий имет дохернена веток и он очень тяжелый
а нам нужна всего одна ветка.

