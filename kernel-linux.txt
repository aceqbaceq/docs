--
"про ulimit"

перед тем как в поде запустить эластик мы запускаем ПРИВИЛИГИРОВАННЫЙ init контейнер
в котором запускаем команду

- name: increase-vm-max-map
        image: busybox
        command: ["sysctl", "-w", "vm.max_map_count=262144"]
        securityContext:
          privileged: true


вопросы:
     -1) что такое привилигированный контейнер
в чем разница между привилигированный и нерпивилигорванный контейнер.
я незнаю ненешаел непонятног.
 я псмотрел что контейнер привилежд что непривилеж у них все одинковкоко в плане cgroup и нейммпейсы. причем нарисовано что cgroup у боих типо контейнеров такой же как у /proc/1
	 однако чтото чно изсчетно что привлидж контейнер имеет доступ
	 ко всем /dev устройствам. а так как userspace чтов контейнерк что на хосте один и тот же то рут контейнера может дедать что хочет 
	 с устройватсами хсосоостата!
	 
	
	
	0) можно ли запустить юзерский процесс без участия баша чтобы типа
	 юлимит недействтвовал
	 1)надо понять под каким юзером работают контейнеры. и что такое 
	 ulimit.
	 3) вылезло то что контейнер зупусается в привилигированром режиме.
	 выянслось что толоком никто непищшет что это значит на уровне low-level. уроды.
	 
	 
	 
	 
	 
вопросы:
	 0) можно ли запустить юзерский процесс без участия баша чтобы типа
	 юлимит недействтвовал
	 1)надо понять под каким юзером работают контейнеры. и что такое 
	 ulimit.
