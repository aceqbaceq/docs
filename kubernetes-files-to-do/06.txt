 я столкнулся

calico-  гавно - не стартует


докер при старте беерте не тот cgroup driver
 detected "cgroupfs" as the Docker cgroup driver. The recommended driver is "systemd".
причем с фланнэлем в итоге это похуй. все равно все поды стратуют на новой ноде




kubelet - если при старте не указал cgroup то он прописывает его в flag-env 
и потом правь не рпавиль в /etc/default похуй. надо статрать flaf-env


flannel рабоатет заебсттись без пробелм


команда на просмотр всех подов по всем нодам по всем неймспейсам к8

~# kubectl get pods --namespace=kube-system -o wide



я помтавил flannel
он на cri-o поднялся.

но осталыне простые поды на на cri=o + f;annel не поднимаются.
вывод -  cri-o  пока что нахуй. чтото там невяжестся.


я убедился что  containerd который входит в состав docker он неподходитдля к8 как отедьный компоненет. 
например crictl отказывается с ним работать


contaiinerd - вставл без проблем.

так что cri- o  в жопу пока.

