| ssh
| reverse


есть к комп-1 к котоому есть доступ в кграфичкский консоли.
туда можно зайти и оттуда зайти на удаелннй ссш сервер-2.
так вот надо сделать так чтобы можйно было в итоге зайти на комп-1 по ссш

делаем так  ссш обратный пркси
заходим на комп-1 и стучим на сервер-2

ssh  -R 2222:localhost:22       user@сервер-2


тогда если зайти по ссш на сервер-2 и сделать 
  ssh 127.0.0.1 -p 22


то мы попадем на комп-1 по ссш !!


еще полезная команда докучи

ncat -l 8888 -k --exec "/usr/bin/ncat 192.168.150.52 8443"



