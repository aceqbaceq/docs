| solaris 10
| netcat

очень помогает когда нужно скопировть файл.
пример процессор UltraSPARC-T1 ( как узнат смотри solaris 10 cpu узнать модель.txt) 
на самоом слабом шифровании ssh он кприрует файл на 2МБ/с
а если чреез netcat то на 12МБ/с


как узнать нагрузку на цпу при копировании смотри в 'solaris 10  cpu load загрузка usage.txt'

как поставить netcat - ставим его как любой другой пакет вот тут 'solaris10-mc.txt'

теперь как пользовться


на сервере(приемнике):
 # /opt/csw/bin/netcat -l -p 9999 -c > /usr/DISTR/all_in_one.zfs


на отправителе
 # nc 192.168.0.205 9999 < ./all_in_one.zfs


как постмреть срупут на солярис 10 по сети хер знает
на фрибсл это просто 

  # systat -ifstat 1

на линуксе 

  # nmon



но можно посмреть можно хортя бы скорсть конекта

  # kstat -m bge -i 0 -s link_speed
  # kstat -m bge -i 0 -s link_duplex


