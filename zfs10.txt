| zfs

есть такая фигня 
типа если непнятно куда ушло место 
то значит что либо место занято под снэпштты

 zfs list -t snapshot

либо есть файлы котоыре есь на фс а потом свержу еще чтото примонтровали и поэтому 
их невиндно тоогда 


Create the mount point:
mkdir /mnt/ghost
Mount the root device to itself:
mount -t nullfs / /mnt/ghost
Check the "hidden" sizes:
du -sh /mnt/ghost/var
du -sh /mnt/ghost/usr
du -sh /mnt/ghost/root

